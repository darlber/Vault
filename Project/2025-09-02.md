---
tags:
  - Proyecto
  - Android
  - Changelog
Date:
  "2025-09-02"
---
1. Añadimos en Session.kt codigo para que no tire error en Android 13+
# Title
```kotlin

```

# Title
```kotlin

```

# Session.kt
```kotlin
  
    val filter = IntentFilter(TimerService.Intents.STATUS.toString())  
  
    if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.TIRAMISU) {  
        // Android 13+ requires explicit receiver flag  
        context.registerReceiver(receiver, filter, Context.RECEIVER_NOT_EXPORTED)  
    } else {  
        @Suppress("DEPRECATION")  
        context.registerReceiver(receiver, filter)  
    }  
  
    context.sendTimerAction(TimerService.Actions.QUERY)  
  
    onDispose { context.unregisterReceiver(receiver) }  
}
```

# Commit Final
