---
tags:
  - Proyecto
  - Android
  - Changelog
Date:
  "2025-07-31"
---
1. pegar el Sessionheader al top (TODO el parallax)
2. añadimos barrita separadora
3. HomeBottomBar que herede de BotttomAppBar para el material 3, color transparente. En vez de manejar eventos como onClick, onOptions, etc. llamamos a Event y a HomeEvent
4. Cambiamos el funOnEvent en HomeViewModel.kt para que poder quitar esos parametros de HomeBottomBar y Home.kt
5. Mostrar barritta abajo y fab buscando el listado de ejercicios

# Commit Final
Refactor: Update session header and home bottom bar  
  
This commit refactors the session header and home bottom bar components.  
  
- **`HeaderSession.kt`**:  
    - Removed fixed height, now uses `wrapContentHeight`.  
    - Added a horizontal line below the session title.  
- **`HomeBottomBar.kt`**:  
    - Refactored to use `BottomAppBar`.  
    - `onEvent` callback now used for button actions.  
- **`Home.kt`**:  
    - Updated `HomeBottomBar` invocation to pass `viewModel::onEvent`.  
- **`HomeViewModel.kt`**:  
    - `onEvent` parameter type changed from `HomeEvent` to `Event`.  
- **`Session.kt`**:  
    - Updated `HeaderSession` invocation, removing the `height` parameter.  
    - `HomeBottomBar` added to the `Scaffold`. Removed background `Surface`.  
- **`AppNavHost.kt`**:  
    - Added a new route for `EXERCISE_PICKER`.  
- **New File**:  
    - `ui/exercisepicker/ExercisePicker.kt` (empty class).


