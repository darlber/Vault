---
tags:
  - Android
  - Proyecto
  - Changelog
---
# Commit Final
feat: Update app launcher icon  
This commit replaces the old application icon with a new one.  
  
Changes include:  
- Removing the old `ic_launcher.xml` and `ic_launcher_round.xml` from the `mipmap-anydpi` directory.  
- Adding new adaptive icon definitions for API v26+.  
- Updating the `AndroidManifest.xml` to use the new `ic_launcher` and `ic_launcher_round` drawables.  
- Adding new foreground, background, and Play Store icon assets.  
- Adding a new `logo_noback.png` drawable.